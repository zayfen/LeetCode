struct Solution();

impl Solution {
  pub fn combination_sum(candidates: Vec<i32>, target: i32) -> Vec<Vec<i32>> {

  }

  pub fn combination_sum_helper (candidates: Vec<i32>, target: i32, path: Vec<i32>, result: Vec<Vec<i32>>) {
    
  }
}

#[cfg(test)]
mod test_array {
  use super::*;

  fn test_combination_sum () {
    
  }
}
